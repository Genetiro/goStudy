1. Явная передача указателя в качестве входных
 параметров и возвращаемых результатов необходима если параметры находятся 
в разных функциях, тогда изменяется значение переменной, а не ее копия.
С указателем или нет, но метод должен уточнить приемник указателя, иначе 
результат останется без изменений.



2. Умножение является инфиксным оператором т.е. находится между двух операндов
и запрашивает два значения, в то время как оператор разыменования 
используется в роли префикса для единственной переменной.